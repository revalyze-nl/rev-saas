# =============================================================================
# PRODUCTION ENVIRONMENT
# =============================================================================
# This file shows the required variables for production deployment
# Actual values should be injected via CI/CD from GitHub Secrets
#
# CRITICAL: Production MUST use LIVE Stripe keys (sk_live_*)
# The application will REFUSE to start if test keys are used in production

APP_ENV=production
APP_PORT=8080

# =============================================================================
# MongoDB Configuration
# =============================================================================
# Database name will be: rev_saas_prod
MONGO_URI=mongodb+srv://your-production-cluster
MONGO_DB_NAME=rev_saas

# =============================================================================
# Authentication
# =============================================================================
JWT_SECRET=${JWT_SECRET}

# =============================================================================
# OpenAI
# =============================================================================
OPENAI_API_KEY=${OPENAI_API_KEY}

# =============================================================================
# Encryption (32 bytes = 64 hex characters)
# =============================================================================
ENCRYPTION_KEY=${ENCRYPTION_KEY}

# =============================================================================
# Stripe Connect (for analytics)
# MUST use LIVE keys (sk_live_*) for production
# =============================================================================
STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
STRIPE_CONNECT_CLIENT_ID=${STRIPE_CONNECT_CLIENT_ID}
STRIPE_CONNECT_REDIRECT_URL=https://api.revalyze.com/api/stripe/connect/callback
APP_PUBLIC_URL=https://revalyze.com

# =============================================================================
# Stripe Billing (subscription payments)
# MUST use LIVE keys (sk_live_*) for production
# =============================================================================
STRIPE_BILLING_SECRET_KEY=${STRIPE_BILLING_SECRET_KEY}
STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}

# URLs for checkout flow
STRIPE_BILLING_SUCCESS_URL=https://revalyze.com/app/settings/billing?success=1
STRIPE_BILLING_CANCEL_URL=https://revalyze.com/app/settings/billing?canceled=1
STRIPE_CUSTOMER_PORTAL_RETURN_URL=https://revalyze.com/app/settings/billing

# Stripe Price IDs (LIVE mode - production uses live prices)
STRIPE_PRICE_STARTER_ID=${STRIPE_PRICE_STARTER_ID}
STRIPE_PRICE_GROWTH_ID=${STRIPE_PRICE_GROWTH_ID}
STRIPE_PRICE_ENTERPRISE_ID=${STRIPE_PRICE_ENTERPRISE_ID}


