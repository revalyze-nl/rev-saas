# =============================================================================
# STAGING ENVIRONMENT
# =============================================================================
# This file shows the required variables for staging deployment
# Actual values should be injected via CI/CD from GitHub Secrets

APP_ENV=staging
APP_PORT=8080

# =============================================================================
# MongoDB Configuration
# =============================================================================
# Database name will be: rev_saas_staging
MONGO_URI=mongodb+srv://your-staging-cluster
MONGO_DB_NAME=rev_saas

# =============================================================================
# Authentication
# =============================================================================
JWT_SECRET=${JWT_SECRET}

# =============================================================================
# OpenAI
# =============================================================================
OPENAI_API_KEY=${OPENAI_API_KEY}

# =============================================================================
# Encryption (32 bytes = 64 hex characters)
# =============================================================================
ENCRYPTION_KEY=${ENCRYPTION_KEY}

# =============================================================================
# Stripe Connect (for analytics)
# MUST use TEST keys (sk_test_*) for staging environment
# =============================================================================
STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
STRIPE_CONNECT_CLIENT_ID=${STRIPE_CONNECT_CLIENT_ID}
STRIPE_CONNECT_REDIRECT_URL=https://staging-api.revalyze.co/api/stripe/connect/callback
APP_PUBLIC_URL=https://staging.revalyze.co

# =============================================================================
# Stripe Billing (subscription payments)
# MUST use TEST keys (sk_test_*) for staging environment
# =============================================================================
STRIPE_BILLING_SECRET_KEY=${STRIPE_BILLING_SECRET_KEY}
STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}

# URLs for checkout flow
STRIPE_BILLING_SUCCESS_URL=https://staging.revalyze.co/app/settings/billing?success=1
STRIPE_BILLING_CANCEL_URL=https://staging.revalyze.co/app/settings/billing?canceled=1
STRIPE_CUSTOMER_PORTAL_RETURN_URL=https://staging.revalyze.co/app/settings/billing

# Stripe Price IDs (TEST mode - staging uses test prices)
STRIPE_PRICE_STARTER_ID=${STRIPE_PRICE_STARTER_ID}
STRIPE_PRICE_GROWTH_ID=${STRIPE_PRICE_GROWTH_ID}
STRIPE_PRICE_ENTERPRISE_ID=${STRIPE_PRICE_ENTERPRISE_ID}


